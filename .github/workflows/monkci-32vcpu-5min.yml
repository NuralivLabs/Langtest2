name: monkci-32vcpu-5min
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/monkci-32vcpu-5min.yml
jobs:
  run:
    runs-on: monkci-ubuntu-24.04-32
    steps:
      - name: 5 minute workload (32 vCPU)
        run: |
          echo "start $(date -u +%FT%TZ) host=$(hostname) nproc=$(nproc)"
          sleep 300
          echo "done $(date -u +%FT%TZ)"
